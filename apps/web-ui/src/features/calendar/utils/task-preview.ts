export type PreviewBar = {
  id: string;
  label: string;
};

export function getPreviewBars(date: Date): PreviewBar[] {
  void date;
  return [];
}

export function getTasksForDateKey(dateKey: string): string[] {
  void dateKey;
  return [];
}

{
  "name": "focus-hybrid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "web:dev": "pnpm -C apps/web-ui dev",
    "web:build": "pnpm -C apps/web-ui build",
    "api:dev": "pnpm -C apps/api dev",
    "api:build": "pnpm -C apps/api build",
    "api:start": "pnpm -C apps/api start",
    "api:prisma:generate": "pnpm -C apps/api prisma:generate",
    "api:prisma:push": "pnpm -C apps/api prisma:push",
    "web:sync-mobile": "node scripts/sync-web-to-mobile.mjs",
    "embed:web-ui": "pnpm web:sync-mobile",
    "hybrid:prepare": "pnpm web:build && pnpm web:sync-mobile",
    "mobile:start": "pnpm -C apps/mobile start -c",
    "mobile:ios": "pnpm -C apps/mobile ios",
    "mobile:android": "pnpm -C apps/mobile android",
    "hybrid:start": "pnpm hybrid:prepare && pnpm mobile:start",
    "hybrid:ios": "pnpm hybrid:prepare && pnpm mobile:ios",
    "hybrid:android": "pnpm hybrid:prepare && pnpm mobile:android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0"
}

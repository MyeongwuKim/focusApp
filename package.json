{
  "name": "focus-hybrid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "web:dev": "pnpm -C apps/web-ui dev",
    "web:storybook": "pnpm -C apps/web-ui storybook",
    "web:storybook:build": "pnpm -C apps/web-ui storybook:build",
    "web:build": "pnpm -C apps/web-ui build",
    "web:type": "pnpm -C apps/web-ui graphql:codegen",
    "api:dev": "pnpm -C apps/api dev",
    "api:build": "pnpm -C apps/api build",
    "api:start": "pnpm -C apps/api start",
    "api:prisma:generate": "pnpm -C apps/api prisma:generate",
    "api:prisma:push": "pnpm -C apps/api prisma:push",
    "web:sync-mobile": "node scripts/sync-web-to-mobile.mjs",
    "native:prepare:web": "pnpm web:build && pnpm web:sync-mobile",
    "native:sync:web": "pnpm native:prepare:web && node scripts/reload-native-app.mjs",
    "native:start": "pnpm -C apps/mobile start -c --dev-client --host lan --port 8081",
    "native:ios:device": "pnpm native:prepare:web && pnpm -C apps/mobile native:sync:test && pnpm -C apps/mobile ios:device",
    "native:ios:local": "pnpm native:prepare:web && pnpm -C apps/mobile native:sync:test && pnpm -C apps/mobile ios:local",
    "native:ios:dev": "pnpm native:prepare:web && pnpm -C apps/mobile native:sync:test && pnpm -C apps/mobile ios:dev",
    "native:ios:prod": "pnpm native:prepare:web && pnpm -C apps/mobile native:sync:prod && pnpm -C apps/mobile ios:prod",
    "native:android:local": "pnpm native:prepare:web && pnpm -C apps/mobile native:sync:test && pnpm -C apps/mobile android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@shopify/react-native-skia"
    ]
  }
}
